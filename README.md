# Dotfiles

This is my dotfile repo... I have 0 idea on how I wan't to actually do this right now.

## How to use:

Run `sudo basic-<DISTRO>.sh` to install some basics I want in every install along with rust and the `just` cli.

Then run `just -l` to see all avaliable recipes to execute from the `justfile`.